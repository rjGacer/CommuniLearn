{
  "name": "communilearn-monorepo-helper",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "bash render-build.sh",
    "build:unsafe": "cd CommuniLearn && npm ci --unsafe-perm=true && npm run build",
    "postinstall": "echo 'Root postinstall: no-op'"
  }
}
